{
  "name": "workspace",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "workspaces": [
    "artifacts/visioninspect-ai",
    "lib/api-client-react"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/visioninspect-ai",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
